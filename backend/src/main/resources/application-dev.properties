# Application Configuration
spring.application.name=${SPRING_APPLICATION_NAME:maae}

# Database Configuration (DEV)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-ancient-resonance-a8lgvodd-pooler.eastus2.azure.neon.tech/maae-dev?user=maae-dev_owner&password=npg_vu4wq3jISlPm&sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:maae-dev_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_vu4wq3jISlPm}
spring.datasource.driver-class-name=org.postgresql.Driver

# DevTools Configuration
spring.devtools.restart.enabled=${SPRING_DEVTOOLS_RESTART_ENABLED:true}
spring.devtools.livereload.enabled=${SPRING_DEVTOOLS_LIVERELOAD_ENABLED:true}
spring.devtools.restart.poll-interval=${SPRING_DEVTOOLS_RESTART_POLL_INTERVAL:2s}
spring.devtools.restart.quiet-period=${SPRING_DEVTOOLS_RESTART_QUIET_PERIOD:1s}

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGIN:http://localhost:3000,http://127.0.0.1:3000,http://localhost:4200,http://127.0.0.1:4200}
